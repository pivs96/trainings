'use strict';

/*angular.module('frontendApp')
  .factory('FeedbackService',['$resource','data', function($resource,data) {
    return $resource('http://localhost:8080/feedback', {}, {
      postFeedback: {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        data: data
      }
    });
  }]);*/
